package seedu.duke.command;

import java.util.ArrayList;

public class MemberCommand {
    public void addMemberCommand(ArrayList<String> names) {
        /*
           For testing purposes only, to be deleted.
         */
        String name =  "";
        for (String n : names) {
            name += n + " ";
        }
        System.out.println(name);


        /* Insert actual code for adding members to the project here.
        .
        .
        .
         */
    }

    public void deleteMemberCommand(ArrayList<String> names) {
        /*
           For testing purposes only, to be deleted.
         */
        String name =  "";
        for (String n : names) {
            name += n + " ";
        }
        System.out.println(name);


        /* Insert actual code for deleting members from the project here.
        .
        .
        .
         */
    }
}
